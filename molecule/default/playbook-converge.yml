---
- name: Build Workstation
  hosts: all
  become: true
  gather_facts: true
  roles:
    - kilip.workstation.build
  vars:
    # only runs this long running tasks when package changes
    brew: "{{ lookup('env', 'WS_BREW') }}"
    regolith: "{{ lookup('env', 'WS_REGOLITH') }}"
