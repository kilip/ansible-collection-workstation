---
- name: Ensure apt chache updated
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 1500
  changed_when: false

- name: Ensure dist upgraded
  ansible.builtin.apt:
    upgrade: dist
  changed_when: false
